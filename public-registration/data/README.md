# Generated sponsor directory

`public-registration/data/sponsors.json` is generated and maintained by the **Sync VISTA Sponsors** GitHub Actions workflow.

Release packages intentionally do not include that file so deploying a new sprint cannot replace the live sponsor directory with placeholder data.
