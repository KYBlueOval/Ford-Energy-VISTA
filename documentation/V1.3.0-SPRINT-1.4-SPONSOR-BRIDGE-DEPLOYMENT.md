# VISTA v1.3.0 Sprint 1.4

This release removes the failed CORS/JSONP sponsor-directory methods and uses only the Apps Script iframe/postMessage bridge.

## Critical deployment check
The live registration page must load `public-registration/app.js?v=13014`.
If DevTools shows `app.js?v=13012`, GitHub is still serving the older release.

## Apps Script
Replace `Code.gs`, save, then deploy a **new web app version**. The bridge action is:
`?action=sponsorBridge&nonce=TEST&origin=https%3A%2F%2Fkyblueoval.github.io`

## GitHub
Upload the **contents at the root of this ZIP**, preserving the folders. Do not upload an enclosing release folder.
